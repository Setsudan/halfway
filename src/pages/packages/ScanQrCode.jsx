// Will scan the shop qr code to confirm command delivery
// Then will scan client qr code to confirm command arrival

export const ScanQrCode = () => {
  return <>{/* Pareil que pour ./src/pages/packages/NearbyCommand.jsx */}</>;
};
